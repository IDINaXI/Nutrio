[build]
builder = "nixpacks"
buildCommand = "cd nutrio_app && flutter build web --release"

[deploy]
startCommand = "cd nutrio_app/build/web && npx serve -s ."
healthcheckPath = "/"
healthcheckTimeout = 100
restartPolicyType = "always"
restartPolicyMaxRetries = 10 